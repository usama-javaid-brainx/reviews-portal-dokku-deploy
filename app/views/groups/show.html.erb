<main class="container overflow-hidden" data-controller="group" data-group-search-value="<%= groups_search_path %>" data-group-newgroup-value="<%= new_group_path %>">
  <%= render "shared/profile_image" %>
  <span class="user-name d-flex justify-content-center profile-name mt-3"><%= @group.name.titleize %></span>
  <div class="row justify-content-center pt_30" data-controller="guest">
    <button type="button" class="col-lg-1 col-md-3 col-8 btn white mx-2" data-bs-toggle="modal" data-bs-target="#shareModal" data-action="click->guest#addLinkField">
      <%= image_tag("template/share.svg") %>Share
    </button>
    <button class='col-lg-1 col-md-3 col-8 mt-3 mt-md-0 btn mx-2' data-groupId="<%= @group.id %>" data-action="click->group#newRequest">
      <%= image_tag("template/edit.svg") %><span>Edit</span>
    </button>
    <%= link_to "Delete", group_path(@group), data: { turbo_method: :delete }, class: "btn delete col-lg-1 col-md-3 col-8 mt-3 mt-md-0 btn mx-2" %>
    <%= render "shared/share_modal" %>
  </div>
  <%= render "new_homepage/review_cards", reviews: @reviews, pagy: @pagy %>
  <div id="groupModalAppend"></div>
</main>
