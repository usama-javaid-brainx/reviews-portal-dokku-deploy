<div class="manage-categories">
  <h3>
    Manage Categories
  </h3>
  <%= form_with url: update_categories_status_path, method: :patch do |form| %>
    <div class="active">
      <h4 class="mt-5 mb-4">Active</h4>
      <ul class="active-list category-list" data-controller="category-reorder">
        <% @categories.each do |category| %>
          <% if category.active? %>
            <li class="category-list-item row">
              <%= form.text_field "positions[#{category.id}]", hidden: true %>
              <!-- <div class="col-1"><%#= show_svg(category.icon) if category.icon.attached?%></div>-->
              <span class="category-name col-9"><%= category.name %></span>
              <div class="col-3 mode-switch d-flex flex-row justify-content-between align-items-center">
                <%= form.check_box "categories[#{category.id}]", id: "#{category.name}-switch", class: "switch", checked: category.active %>
                <label for=<%= "#{category.name}-switch" %>></label>
                <%= image_tag('template/reorder.svg') %>
              </div>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
    <div class="inactive">
      <h4>Inactive</h4>
      <ul class="inactive-list category-list" data-controller="category-reorder">
        <% @categories.each do |category| %>
          <% unless category.active? %>
            <li class="category-list-item row">
              <%= form.text_field "positions[#{category.id}]", hidden: true %>
              <!-- <div class="col-1"><%#= show_svg(category.icon) if category.icon.attached?%></div>-->
              <span class="category-name col-9"><%= category.name %></span>
              <div class="col-3 mode-switch d-flex flex-row justify-content-between align-items-center">
                <%= form.check_box "categories[#{category.id}]", id: "#{category.name}-switch", class: "switch", checked: category.active %>
                <label for=<%= "#{category.name}-switch" %>></label>
                <%= image_tag('template/reorder.svg') %>
              </div>
            </li>
          <% end %>
        <% end %>
      </ul>
    </div>
    <%= form.submit "Save Changes", class: 'save-changes d-block ms-auto' %>
  <% end %>
</div>

<div class="manage-categories d-flex">
  <div class="col-8">
    <h3>
      Don’t see categories for you?
    </h3>
    <p class="request-catagory">Let us know about that. We constantly develop our app and we're looking for new
      solutions.</p>
  </div>
  <div class="col-4 m-auto">
    <button type="button" class="request-btn d-block ms-auto" data-bs-toggle="modal" data-bs-target="#request-modal">
      Send Request
    </button>

    <!-- Request Modal -->
    <div class="modal fade" id="request-modal" tabindex="-1" aria-labelledby="requestModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <div class="modal-content">
          <div class="modal-header d-block">
            <div class="header-title d-flex align-items-center">
              <h2 class="modal-title" id="requestModalLabel">New Category Request</h2>
              <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <p class="header-para">Tell us more about your needs. Enter the name of the new category and describe a bit
              your requirements.</p>
          </div>
          <div class="modal-body">
            
          </div>
          <div class="modal-footer justify-content-between">
            <button type="button" class="cancel" data-bs-dismiss="modal">Cancel</button>
            <button type="button" class="save-changes" data-bs-dismiss="modal">Send Request</button>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>