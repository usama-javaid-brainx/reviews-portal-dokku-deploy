<div class="row">
  <div class="col-lg-12">
    <h2>Category</h2>
    <div data-controller="category" class="category_content_wrap">
      <div class="category-group form_group row align-items-center">
        <label for="search">Category</label>
        <%= f.select :category_id, @ordered_categories.collect { |c| [c.name, c.id] }, { selected: @curr_category.id }, { data: { "action": "change->category#categorySelect", "category-target": "category" } } %>
      </div>
      <div class="category-group row align-items-center">
        <label for="search">Type</label>
          <%= f.select :to_try, [["Review", false], ["Wishlist", true]], { include_blank: false }, class:"text-black", data: { "action": "change->category#categorySelect", "category-target": "category" } %>
      </div>
    </div>
  </div>
</div>
